/**
 * lib/adapters/postgres.ts
 * Auth.js v5 adapter for generic Postgres (self-hosted Docker deployments).
 *
 * Uses the official @auth/drizzle-adapter with DataBreef's Drizzle instance.
 * Requires only DATABASE_URL — works with any Postgres instance.
 *
 * Install: pnpm add @auth/drizzle-adapter
 */

// import { DrizzleAdapter } from "@auth/drizzle-adapter";
// import { db } from "../db";

// export const postgresAdapter = DrizzleAdapter(db);

export {};
